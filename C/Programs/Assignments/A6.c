
// WAP to take amount in INR and convert it in USD assuming 1USD = 84.23
#include<stdio.h>
/*
int main()
{
    int n;
    printf("Enter an amount in INR: \n");
    scanf("%d",&n);
    printf("Amount of %d in INR is equivalent to %.3f in USD",n,n/84.23);
    return 0;
}
    
// WAP to take 3 digit number from the user and rotate its digit by one position towards right
int main()
{
    int n, nr;
    printf("Enter a 3 digit number: \n");
    scanf("%d",&n);
    nr = n%100*10+n/100;
    printf("Rotation of %d digit by one position towards right is: %d",n,nr);
}

// WAP to take 3 digit number from the user and rotate its digit by one position towards right
int main()
{
    int n, nr;
    printf("Enter a 3 digit number: \n");
    scanf("%d",&n);
    nr = n%10*100+n/10;
    printf("Rotation of %d digit by one position towards right is: %d",n,nr);
}

// What will be value stored in variable x after executing x = 10>8>4; 
// Answer: 0
int main()
{
 
int x;
    x = 10>8>4;
    printf("Variable x = %d",x);
    return 0;
}
   

// What will be value stored in variable x after executing x = !2>-2; 
// Answer: !2 = 0 > -2 > 1
int main()
{
    int x;
    x = !2>-2;
    printf("Variable x = %d",x);
    return 0;
}

 */
// What will be value stored in variable x after executing x = 3<0&&5>0; 
// Answer: 3<0 :> 0, 5>0:>1 x = 0&&1:> 0. Final Answer = 0
int main()
{
    int x;
    x = 3<0 && 5>0;
    printf("Variable x = %d",x);
    return 0;
}


